{
  "name": "@sketchbattle/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "node seed.js",
    "studio": "prisma studio"
  },
  "dependencies": {
    "prisma": "^6.5.0",
    "@prisma/client": "^6.5.0",
    "bcrypt": "^5.1.1"
  }
}
